name = "icij-offshore-leaks-investigator"
version = "1.0.0"
description = "Investigative knowledge graph for ICIJ Offshore Leaks database"
entry-point = "main.jac"

[dev-dependencies]
watchdog = ">=3.0.0"

[serve]

[plugins.scale.cors]
allow_origins = ["*"]
allow_methods = ["GET", "POST", "OPTIONS"]
allow_headers = ["*"]

[jacpack]
name = "icij-offshore-leaks-investigator"
description = "ICIJ Offshore Leaks investigative knowledge graph"
